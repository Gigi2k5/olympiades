# ══════════════════════════════════════════════════════
# Configuration Olympiades IA Bénin - Backend
# Copier ce fichier vers .env et remplir les valeurs
# ══════════════════════════════════════════════════════

# === Base de données ===
# PostgreSQL (recommandé)
DATABASE_URL=postgresql://olympiades_user:olympiades_pass@localhost:5432/olympiades_ia_dev
# Pool de connexions (ajuster selon la charge)
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20

# === Sécurité ===
# Générer avec: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-in-production
JWT_SECRET_KEY=change-this-jwt-key-in-production

# === JWT ===
JWT_ACCESS_TOKEN_EXPIRES=3600
JWT_REFRESH_TOKEN_EXPIRES=2592000

# === CORS ===
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# === Environnement ===
FLASK_ENV=development
FLASK_DEBUG=1

# === Redis (recommandé en production) ===
# Utilisé pour: rate limiting multi-worker + blacklist JWT
# Sans Redis, fallback en mémoire locale (ne fonctionne pas en multi-worker)
REDIS_URL=redis://localhost:6379/0

# === Email Brevo (ex-Sendinblue) ===
BREVO_API_KEY=
BREVO_SENDER_EMAIL=contact@olympiades-ia.bj
BREVO_SENDER_NAME=Olympiades IA Bénin

# URL du frontend (pour les liens dans les emails)
FRONTEND_URL=http://localhost:5173

# === Stockage fichiers ===
# 'local' pour dev (disque), 's3' pour production
STORAGE_BACKEND=local

# Configuration S3 (requise si STORAGE_BACKEND=s3)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=olympiades-ia-uploads
AWS_S3_REGION=eu-west-3
# AWS_S3_ENDPOINT=  # Pour MinIO ou S3-compatible
# CDN_URL=          # URL CDN devant S3 (optionnel)

# === Admin par défaut ===
ADMIN_EMAIL=admin@olympiades-ia.bj
ADMIN_PASSWORD=OlympiadesIA2026!
